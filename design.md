# تصميم واجهة تطبيق «جدولي»

## الرؤية

«جدولي» هو محرّر جداول بيانات عربي مبسّط للاستخدام اليومي على الهاتف. يركّز التصميم على العمل بيد واحدة في الوضع العمودي 9:16، مع شبكة خلايا واضحة، شريط صيغ دائم، ووصول سريع إلى الحسابات المتكررة دون إرباك المستخدم بعناصر سطح مكتب مصغّرة.

## هوية الواجهة

تستخدم الواجهة أزرقاً مائلاً إلى النيلي (`#2457E5`) كلون أساسي يوحي بالدقة والثقة، وخلفية بيضاء مزرقة (`#F7F9FE`) لتقليل إجهاد النظر، وسطحاً أبيض (`#FFFFFF`) للبطاقات والجدول. تُعرض النجاحات بلون أخضر متزن (`#16865B`) والتنبيهات بلون برتقالي دافئ (`#C76B00`). النصوص الرئيسة كحلية داكنة (`#13213A`) والنصوص الثانوية رمادية مزرقة (`#64748B`).

## قائمة الشاشات

| الشاشة | المحتوى الرئيسي | الوظائف |
|---|---|---|
| ورقة العمل | عنوان الملف، شريط الصيغ، شبكة أعمدة وصفوف، ورقة سفلية | تحديد الخلية، الكتابة، تعديل القيمة أو الصيغة، التنقل بين الخلايا |
| محرر الخلية | اسم الخلية والقيمة المدخلة ولوحة أرقام/نص | تأكيد التعديل أو إلغاؤه، إدخال الصيغ التي تبدأ بـ `=` |
| أدوات الحساب | اختصارات جمع وطرح وضرب وقسمة ومتوسط | إدراج صيغة في الخلية المحددة مع توضيح الاستخدام |
| إدارة الأوراق | تبويب الورقة الحالية وزر إضافة ورقة | إنشاء ورقة جديدة والانتقال بين الأوراق |
| إجراءات الجدول | قائمة إجراءات سياقية مختصرة | إضافة صف أو عمود، مسح محتوى خلية، التراجع عن آخر تعديل |

## تدفق الاستخدام الأساسي

| الإجراء | المسار |
|---|---|
| إدخال قيمة | يلمس المستخدم خلية ← تظهر حالة التحديد ← يكتب القيمة في شريط الصيغ أو المحرر ← يؤكد ← تظهر القيمة في الخلية |
| إجراء حساب | يختار خلية النتيجة ← يفتح أدوات الحساب ← يختار عملية ← يكتب مراجع الخلايا، مثل `A1+B1` ← يؤكد ← تظهر النتيجة |
| استخدام دالة | يختار خلية ← يكتب `=SUM(A1:A5)` ← يؤكد ← تعرض الخلية الناتج وتتحدث مع تعديل القيم |
| إنشاء ورقة | يلمس زر الإضافة بجانب تبويبات الأوراق ← تُنشأ ورقة جديدة باسم متسلسل ← ينتقل التطبيق إليها |
| إدارة مساحة العمل | يفتح قائمة الأدوات ← يختار إضافة صف/عمود أو حذف محتوى الخلية ← تتحدث الشبكة فوراً |

## قواعد التخطيط والتفاعل

يبقى عنوان المصنف وشريط الصيغ في أعلى الشاشة داخل المنطقة الآمنة. يشغل الجدول المساحة المرئية الأساسية، مع رؤوس أعمدة ثابتة ومساحة لمس لا تقل عن 44 نقطة للأزرار. تعرض الخلية المحددة حدّاً أزرقاً واضحاً مع مؤشرات سحب، بينما تظهر الصيغ في شريط الصيغ والناتج داخل الشبكة. تُستخدم تأثيرات ضغط خفيفة وتغذية لمسية فقط للأفعال الأساسية مثل إضافة ورقة أو حفظ تعديل.

## نموذج البيانات المحلي

يعتمد التطبيق على ملف عمل محلي يتضمن مجموعة أوراق. تحتوي كل ورقة على معرف واسم وأبعاد وعدد من الخلايا المخزنة بواسطة مرجع مثل `A1`. تحفظ كل خلية النص الأصلي (`raw`) والقيمة المعروضة (`display`) ونوعها: نص أو رقم أو صيغة أو خطأ. يحفظ التطبيق هذه البنية محلياً كي تبقى البيانات بعد إغلاق التطبيق.

